#include <iostream>
#include <cmath>
#include <cstdio>
#include <algorithm>

using namespace std;
typedef long long ll;
ll di[100001];

ll num(ll n,ll k)
{
    ll m=sqrt(n), cnt=0;
    for(int i=1;i<=m;++i){
        if(n%i==0){
        }
    }
}

int main()
{
    freopen("kthdiv.inp","r",stdin);
    freopen("kthdiv.out","w",stdout);
    ll n,k;
    cin>>n>>k;
    cout<<num(n,k);
    return 0;
}
